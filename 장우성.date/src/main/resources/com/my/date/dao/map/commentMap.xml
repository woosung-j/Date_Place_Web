<?xml version='1.0' encoding='utf-8' ?>
<!DOCTYPE mapper PUBLIC '-//mybatis.org//DTD Mapper 3.0//EN'
	'http://mybatis.org/dtd/mybatis-3-mapper.dtd'>
<mapper namespace='com.my.date.dao.map.CommentMap'>
	<select id='selectComments' resultType='commentDto'>
		select c.feed_id, c.comment_id, c.created_at, c.content, c.user_id  u.nickname
		from comments c, users u
		where c.user_id = u.user_id and c.feed_id = #{feedId}
		order by comment_id
	</select>
</mapper>