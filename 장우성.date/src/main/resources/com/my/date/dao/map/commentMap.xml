<?xml version='1.0' encoding='utf-8' ?>
<!DOCTYPE mapper PUBLIC '-//mybatis.org//DTD Mapper 3.0//EN'
	'http://mybatis.org/dtd/mybatis-3-mapper.dtd'>
<mapper namespace='com.my.date.dao.map.CommentMap'>
	<select id='selectComments' resultType='commentDto'>
		select f.feed_id, c.comment_id, c.created_at, c.content, c.feed_id 
		from comments c, feeds f
		where c.feed_id = #{feedId} 
		and c.feed_id = f.feed_id
		order by comment_id
	</select>
	
	<resultMap id='commentMap' type='CommentDto'>
		<id property='commentId' column='comment_id' />
		<result property='feedId' column='feed_id' />
		<result property='userId' column='user_id' />
		<result property='content' column='content' />
		<result property='createdAt' column='created_at' />
		<result property='nickname' column='nickname' />
	</resultMap>
</mapper>		